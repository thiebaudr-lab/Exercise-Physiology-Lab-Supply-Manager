# ExPhys LIMS — Log Sheet Importer dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# On Windows you also need poppler in PATH for pdf2image:
#   https://github.com/oschwartz10612/poppler-windows/releases
#   Extract and add the Library/bin folder to your PATH.
#
# On Mac:
#   brew install poppler

# GUI
customtkinter>=5.2.0

# OCR (works on Python 3.8–3.14)
easyocr>=1.7.0

# PDF rasterisation
pdf2image>=1.17.0
Pillow>=10.0.0

# Numeric / array (installed automatically with easyocr)
numpy>=1.24.0

# Build (optional — only needed to create .exe / .app)
# pyinstaller>=6.0.0
